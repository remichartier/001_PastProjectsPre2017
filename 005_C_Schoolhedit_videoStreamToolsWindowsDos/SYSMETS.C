/*------------------------------------------------------------------
	 SYSMETS.C -- Affichage de la taille par defaut des objets Windows
		Version definitive
		(c) Charles Petzold, 1992
	------------------------------------------------------------------*/

#define SYSMETS_C
#include "sysmets.h"
#undef SYSMETS_C

//#define min(a,b) (((a) < (b)) ? (a) : (b))
//#define max(a,b) (((a) > (b)) ? (a) : (b))



void main()
{
PHANDLE pMpegFile;
u8 * pVideoBuffer;
 BYTE ret;
					ret=Iso11172_stream((PHANDLE) pMpegFile,(LPHANDLE)  pVideoBuffer);

}